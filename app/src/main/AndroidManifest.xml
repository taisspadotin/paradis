<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taisspadotin.receitas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Receitas" />
        <activity android:name=".Frases" />
        <activity android:name=".pagina02" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Favoritos" />
        <activity android:name=".F1" />
        <activity android:name=".Literatura" />
        <activity android:name=".AutoAjuda"></activity>
    </application>

</manifest>